'use strict';

module.exports = {
	ServerModuleName: 'UserGroups',
	HashModuleName: 'user-groups',

	/**
	 * Initializes settings from AppData object sections.
	 * 
	 * @param {Object} oAppData Object contained modules settings.
	 */
	init: function (oAppData)
	{
		
	}
};
